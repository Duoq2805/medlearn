spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medlearn_db
    username: medlearn
    password: Medlearn@123    # Phải khớp với docker-compose

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.medlearn: DEBUG
    org.hibernate.SQL: DEBUG